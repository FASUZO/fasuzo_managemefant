# 应用端口
PORT=3000

# 自定义数据目录（可写绝对路径或相对项目根目录的相对路径）
DATA_DIR=data

# JSON 请求体大小上限（单位可用 kb、mb 等）
JSON_LIMIT=50mb

# 日志级别：debug | info | error
LOG_LEVEL=info

# ---------- 前端默认行为 ----------
# 是否默认暗黑模式 (true/false)
DEFAULT_DARK=false

# 是否默认启用自动保存 (true/false)
DEFAULT_AUTO_SAVE=false

# 前端 DEBUG 默认开关 (true/false)
DEFAULT_DEBUG=false

# 可选：字体文件 URL（如 CDN 或本地 /fonts.css）
FONT_URL=